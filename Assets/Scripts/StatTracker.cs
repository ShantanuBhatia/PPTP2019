using System.Collections;
using System.Collections.Generic;
using UnityEngine;



public class StatTracker : MonoBehaviour
{
    private float stanDesperation;
    private float towniesBaseConviction;
    
    /*
     * Stan Desperation starts out at some level
     * Each Townie gets their conviction by applying a modifier to BaseConviction
     * 
     */

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
