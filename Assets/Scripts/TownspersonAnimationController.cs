using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TownspersonAnimationController : MonoBehaviour
{
    /*
     * Takes information from the Townsperson Controller in order to set the boolean flags for the animator attached to this object.
     */


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
